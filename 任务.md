你现在是这个项目的首席科研软件工程师。请在当前工作目录中创建并尽可能运行一个完整、可复现、适合公开到 GitHub 的计算蛋白设计 mini-project。

项目名称：

rso-protein-design-exploration

项目目标：

1. 复现 Frank et al. 2024 Science 论文中的 Relaxed Sequence Optimization（RSO）核心计算流程。
2. 完成一条最小闭环：

RSO backbone generation
→ ProteinMPNN sequence design
→ AlphaFold/ColabDesign structure prediction
→ designed backbone 与 predicted structure 比较
→ 输出 RMSD、TM-score、pLDDT 等指标

3. 在复现成功后，探索 protein length 对设计结果和计算成本的影响。
4. 最终生成一个可以用于申请 Dietz Lab computational HiWi 的高质量公开仓库。
5. 项目重点是“正确、可复现、可解释”，不是重新实现 AlphaFold、ProteinMPNN 或 ColabDesign。

====================
一、必须遵守的工作原则
===========

1. 不要凭记忆编写 ColabDesign API。

开始编码前，必须读取并检查以下当前官方资源：

* 论文免费全文：
  https://pmc.ncbi.nlm.nih.gov/articles/PMC11734486/

* ColabDesign：
  https://github.com/sokrypton/ColabDesign

* 当前 RSO notebook：
  https://github.com/sokrypton/ColabDesign/blob/main/af/examples/RSO.ipynb

* 可直接运行的官方 Colab：
  https://colab.research.google.com/github/sokrypton/ColabDesign/blob/main/af/examples/RSO.ipynb

* ProteinMPNN integration：
  https://github.com/sokrypton/ColabDesign/tree/main/mpnn

* 论文代码快照：
  https://doi.org/10.5281/zenodo.13309081

* 论文数据：
  https://doi.org/10.6084/m9.figshare.27009724

* 实验结构 PDB 8S89：
  https://www.rcsb.org/structure/8S89

先提取官方 RSO notebook 实际使用的：

* 安装步骤
* ColabDesign commit 或版本
* AlphaFold 参数下载方式
* RSO model initialization
* loss configuration
* optimization schedule
* ProteinMPNN 设置
* validation 方法
* RMSD、TM-score、pLDDT 的计算或读取方法
* PDB 保存方式

然后再根据实际 API 编码。不要编造不存在的函数、参数或输出字段。

2. 官方 notebook 是方法真相来源。

优先把官方 notebook 保存为只读参考副本，并基于它制作整理版 notebook。不要一开始就把流程完全重写成自定义框架。

3. 禁止伪造结果。

如果当前机器没有合适的 NVIDIA GPU、CUDA、足够显存、模型参数或网络权限：

* 仍然完成目录、notebook、脚本、配置、README 和测试；
* 明确标记哪些步骤尚未实际运行；
* 生成 COLAB_RUNBOOK.md，告诉用户如何在 Google Colab 中完成 GPU 部分；
* 不得生成看起来像真实实验结果的假 CSV、假图或假 PDB；
* 示例数据必须明显标记为 synthetic/demo，并且不得放进正式 results；
* 不得在 README 中声称 reproduction successful。

4. 不要覆盖用户已有文件。

如果当前目录不是空目录，先查看内容和 git 状态。项目应创建在独立的 rso-protein-design-exploration/ 目录中。不要删除、重置或修改不相关文件。

5. 所有耗时实验必须支持断点续跑。

每个设计任务完成后立即写入独立目录和状态文件。重启后跳过已成功完成且文件校验通过的任务。

6. 所有科学结论必须来自真实结果。

README 的 Results 和 Discussion 在没有真实数据时必须写成 “Pending execution”。不要把预期趋势写成观察结果。

7. 每条给用户执行的终端命令都要解释。

在 RUNBOOK.md 中，对每条命令说明：

* 命令整体做什么；
* 每个主要参数的含义；
* 应该在哪个目录运行；
* 预期产生什么文件；
* 如何判断成功；
* 常见错误如何处理。

====================
二、第一阶段：环境审计
===========

先执行只读检查，并把结果写入 docs/environment_audit.md：

* 操作系统与版本
* CPU
* 内存
* 剩余磁盘空间
* Python 版本
* conda/mamba 是否可用
* git 版本
* 是否存在 nvidia-smi
* GPU 型号
* GPU 显存
* CUDA driver/runtime
* JAX 是否安装
* JAX 是否识别 GPU
* 当前目录是否属于 git 仓库
* 当前目录是否有用户未提交修改
* 是否能够访问官方 GitHub 仓库和模型参数地址

根据检查结果选择执行后端：

A. 本地 Linux + 可用 NVIDIA GPU：
建立本地运行环境，并尝试运行 MVP。

B. 没有合适 GPU，但可以使用 Google Colab：
准备完整的 Colab notebook 和下载/同步结果说明，不要尝试在 CPU 上运行正式 RSO。

C. 环境信息不足：
完成所有静态工作和轻量测试，生成清楚的阻塞说明。

不要因为没有 GPU 就停止整个任务。

====================
三、项目目录
======

创建以下结构。可以根据官方 notebook 的实际情况小幅调整，但不得无理由省略核心部分：

rso-protein-design-exploration/
├── README.md
├── LICENSE
├── CITATION.cff
├── .gitignore
├── environment.yml
├── requirements-lock.txt
├── configs/
│   ├── smoke.yaml
│   ├── reproduction_100aa.yaml
│   ├── length_experiment_quick.yaml
│   └── length_experiment_full.yaml
├── notebooks/
│   ├── 00_official_RSO_reference.ipynb
│   ├── 01_rso_reproduction.ipynb
│   ├── 02_length_experiment.ipynb
│   └── 03_results_analysis.ipynb
├── scripts/
│   ├── check_environment.py
│   ├── run_rso.py
│   ├── run_mpnn.py
│   ├── validate_designs.py
│   ├── run_experiment.py
│   ├── collect_metrics.py
│   ├── make_figures.py
│   └── verify_outputs.py
├── src/
│   └── rso_exploration/
│       ├── **init**.py
│       ├── config.py
│       ├── paths.py
│       ├── metrics.py
│       ├── provenance.py
│       └── plotting.py
├── tests/
│   ├── test_config.py
│   ├── test_metrics.py
│   ├── test_paths.py
│   └── test_result_schema.py
├── data/
│   ├── README.md
│   └── external/.gitkeep
├── results/
│   ├── README.md
│   ├── runs/.gitkeep
│   └── metrics/.gitkeep
├── figures/
│   └── .gitkeep
└── docs/
├── METHOD.md
├── RUNBOOK.md
├── COLAB_RUNBOOK.md
├── METRICS.md
├── TROUBLESHOOTING.md
├── environment_audit.md
├── provenance.md
└── scientific_notes.md

如果官方 notebook 只能可靠地在 Colab 环境运行，允许把 GPU 主流程保留在 notebook 中，而让 scripts/ 负责配置生成、结果收集、验证和画图。不要为了“脚本化”而引入未经验证的 API。

====================
四、版本与来源追踪
=========

必须记录：

* ColabDesign repository URL
* 实际使用的 git commit SHA
* RSO notebook 的下载日期
* notebook 文件 SHA256
* Python 版本
* JAX/JAXLIB 版本
* CUDA 版本
* GPU 型号
* AlphaFold 参数文件来源
* ProteinMPNN weights 类型
* 运行时间
* 随机种子
* 完整配置文件
* 每次运行的 git commit
* 是否对官方 notebook 做了修改

把这些信息分别写入：

* docs/provenance.md
* 每个 run 目录下的 metadata.json

如果 main 分支 API 与论文时期版本不兼容，按以下顺序处理：

1. 先记录错误；
2. 检查当前官方 RSO notebook；
3. 检查论文 Zenodo 代码快照；
4. 选择能够运行的版本；
5. 在 README 中解释为什么固定到该 commit/version；
6. 不要静默改变版本。

====================
五、配置设计
======

所有重要实验参数必须来自 YAML 配置，而不是散落在 notebook 中。

每个配置至少包含：

* experiment_name
* backend
* seed
* length
* copies
* rso_iterations
* RSO optimization stages
* loss terms
* loss weights
* ProteinMPNN candidate number
* ProteinMPNN temperature
* ProteinMPNN weight type
* removed amino acids
* validation model
* recycle count（如果适用）
* output directory
* overwrite policy
* resume policy

配置文件要求：

1. smoke.yaml

仅用于验证代码路径，不能用作科学结果。使用非常小的任务或 mock 输入；清楚标注 smoke test 不能代表设计质量。

2. reproduction_100aa.yaml

目标：

* length = 100
* copies = 1
* 采用当前官方 RSO notebook 与论文兼容的 loss 设置
* RSO 正式优化步数以论文/官方 notebook 为准，预期为 100 steps，但必须核实后填写
* 每个 backbone 生成 8 条 ProteinMPNN sequences
* ProteinMPNN temperature = 0.1
* 使用 soluble weights（若当前官方 notebook与论文确实如此）
* 移除 cysteine（若官方流程确实如此）
* 使用官方 notebook 支持的 AlphaFold/ColabDesign validation
* 固定随机种子并记录

3. length_experiment_quick.yaml

用于快速得到第一版真实结果：

* lengths: [100, 150, 200]
* 每个长度 1 个 backbone
* 每个 backbone 8 条 ProteinMPNN sequences
* 其余设置尽可能保持一致

4. length_experiment_full.yaml

用于有足够 GPU 时间后运行：

* lengths: [100, 150, 200, 300]
* 每个长度 3 个独立随机种子
* 每个 backbone 8 条 ProteinMPNN sequences
* 除 length 和 seed 外，其余设置保持一致

如果 300 aa 对可用 GPU 显存不安全，不要强行运行；应报告资源限制并保留配置。

====================
六、核心计算流程
========

对每个 run 实现并记录以下阶段：

Stage 1：RSO backbone generation

输入：

* length
* copies
* random seed
* loss configuration
* optimization schedule

输出：

* designed backbone PDB
* RSO 最终 sequence representation 或可用的 sequence 输出
* optimization history
* 每一步或合理间隔的 loss
* 各 loss component
* 运行时间
* metadata.json
* status.json

Stage 2：ProteinMPNN sequence generation

输入：

* designed backbone PDB

输出：

* 8 条候选序列
* FASTA
* CSV/JSON 格式的序列及分数
* temperature
* weights 类型
* removed amino acids
* random seed
* 运行时间

Stage 3：independent structure prediction / validation

对每条 ProteinMPNN sequence：

* 从序列重新预测结构；
* 保存 predicted PDB；
* 保存模型置信度；
* 将 prediction 与 designed backbone 对齐比较；
* 计算或提取 RMSD；
* 计算 TM-score；
* 计算 mean pLDDT；
* 如果当前 API 支持，则记录 pTM；
* 记录是否失败、失败阶段和错误信息。

如果官方 RSO notebook 的 validation 不是 ESMFold，而是 AlphaFold/ColabDesign，不要擅自改成 ESMFold。README 必须准确写实际使用的 validation model。

Stage 4：ranking

为每个 backbone 对候选序列排序。

不要武断地把不同量纲直接相加。默认至少输出以下独立排名：

* lowest RMSD
* highest TM-score
* highest mean pLDDT

可选地提供 composite ranking，但必须：

* 明确公式；
* 明确归一化方法；
* 说明这是项目自定义 ranking，不是论文官方指标；
* 同时保留所有原始指标。

====================
七、统一结果格式
========

生成 results/metrics/all_candidates.csv，至少包含：

* experiment_name
* run_id
* timestamp
* length
* copies
* seed
* backbone_id
* candidate_id
* sequence
* sequence_length
* rso_iterations
* rso_final_loss
* mpnn_score
* mpnn_temperature
* validation_model
* rmsd_angstrom
* tm_score
* mean_plddt
* ptm（如果存在）
* rso_runtime_seconds
* mpnn_runtime_seconds
* validation_runtime_seconds
* total_runtime_seconds
* gpu_name
* peak_gpu_memory_mb（如果可以获取）
* colabdesign_commit
* git_commit
* status
* error_message

要求：

* 缺失值使用空值/NA，不要写 0；
* 单位必须体现在列名或 METRICS.md 中；
* CSV 中每一行对应一个 sequence candidate；
* 额外生成 backbone_summary.csv，每个 backbone 一行；
* summary 至少包括 best RMSD、best TM-score、best pLDDT、成功候选数和总运行时间。

====================
八、图表
====

只有在存在真实结果时才画正式图。

至少生成：

1. figures/rmsd_vs_length.png
2. figures/tmscore_vs_length.png
3. figures/plddt_vs_length.png
4. figures/runtime_vs_length.png
5. figures/optimization_loss_100aa.png
6. figures/candidate_metrics_100aa.png

图表要求：

* 明确轴名称和单位；
* 标明样本量；
* 多个 seed 时显示所有数据点，不能只画均值；
* 可以增加均值/中位数和误差范围；
* 样本量太小时不得暗示统计显著性；
* 不绘制不存在的数据；
* PNG 使用适合 GitHub 阅读的分辨率；
* 同时保存画图脚本。

结构图如果无法可靠自动渲染，可以在 README 中留下明确步骤，要求用户使用 Mol*、PyMOL 或 ChimeraX 截图。不要制作假的结构示意图冒充结果。

====================
九、测试与验证
=======

实现不需要 GPU 的自动化测试：

* YAML 配置校验
* 路径生成
* run_id 唯一性
* metadata schema
* metrics CSV schema
* 缺失值处理
* ranking 逻辑
* 已完成任务跳过逻辑
* 损坏/不完整输出识别
* 长度与序列长度一致性检查
* PDB 文件非空且包含 ATOM records 的基本检查

如果环境允许，运行：

* Python syntax check
* pytest
* notebook cell syntax/static validation
* import smoke test
* scripts --help
* 配置 dry-run

创建 verify_outputs.py。它应检查：

* 必需文件是否存在；
* PDB 是否有效；
* FASTA 是否存在且序列长度合理；
* metrics 是否包含要求列；
* 每个 candidate 是否有对应 prediction；
* status 是否与文件一致；
* 图表是否来自真实 metrics；
* provenance 是否完整。

只有 verify_outputs.py 通过后，才能在 README 中把某次实验标为 completed。

====================
十、README 内容
===========

README 必须用专业、简洁的英文撰写，包含：

1. Title
2. One-paragraph summary
3. Scientific motivation
4. What is Relaxed Sequence Optimization?
5. Pipeline overview
6. Reproduction target
7. Length experiment
8. Repository structure
9. Installation
10. Running on Google Colab
11. Running locally
12. Configuration
13. Output files
14. Metrics
15. Results
16. Observations
17. Limitations
18. Questions raised
19. Reproducibility and provenance
20. References
21. License

README 的科学解释应准确区分：

* sequence
* backbone
* structure prediction
* inverse folding
* RSO
* ProteinMPNN
* validation
* computational confidence
* experimental validation

必须明确说明：

* 高 pLDDT 不等于实验验证成功；
* prediction 与 design 一致不等于蛋白一定能表达、可溶或稳定；
* 同类 structure-prediction model 参与设计和评价可能产生方法相关偏差；
* 本项目不包含湿实验验证；
* 除非真的运行成功，否则不要声称复现了论文结果。

README 中加入以下 research questions，但根据真实结果调整措辞：

* How does candidate quality change with protein length under a fixed RSO budget?
* Why can a sequence have high prediction confidence but poor agreement with the designed backbone?
* How sensitive are conclusions to the validation model?
* To what extent can using related structure-prediction models for design and evaluation introduce bias?
* How might experimental feedback be incorporated into candidate ranking?

引用至少包括：

* Frank et al., Science 2024
* ColabDesign
* ProteinMPNN
* AlphaFold（如果实际使用）
* ESMFold（仅在实际使用时）
* Figshare dataset
* Zenodo archive

不要复制论文原文；用自己的语言总结。

====================
十一、notebook 质量要求
================

01_rso_reproduction.ipynb：

* 开头说明目标和计算成本；
* 检查 GPU；
* 安装/导入经过固定版本的依赖；
* 设置 seed；
* 加载 reproduction_100aa.yaml；
* 运行或调用 RSO；
* 运行 ProteinMPNN；
* validation；
* 展示 metrics；
* 展示一个最佳结构；
* 保存全部结果；
* 每个重要 cell 前用 Markdown 解释生物学意义；
* 不把大型模型文件提交到 git。

02_length_experiment.ipynb：

* 读取 quick/full 配置；
* 支持选择 profile；
* 支持断点续跑；
* 清晰显示当前 length、seed、阶段和预计任务数；
* 每完成一个候选立即保存；
* 失败不应让整个实验丢失；
* 汇总结果但不夸大统计意义。

03_results_analysis.ipynb：

* 只读取已经保存的 metrics；
* 不重新运行模型；
* 数据质量检查；
* 画图；
* 对失败任务单独报告；
* 自动生成一份 results/analysis_summary.md；
* 如果没有真实数据，应明确显示 “No completed experimental runs found”。

00_official_RSO_reference.ipynb：

* 保存官方 notebook 原始副本；
* 不修改内容；
* 在 docs/provenance.md 记录来源、下载日期和 SHA256；
* 如果许可证不允许直接再分发，则不要复制进仓库，改为保存下载脚本与链接，并在文档解释。

====================
十二、Colab 工作流
============

生成一个适合上传到 Google Colab 的 notebook。

它应当：

* 检查是否真的获得 GPU；
* 打印 GPU 型号和显存；
* 如果没有 GPU，立即停止正式计算并给出清楚提示；
* 可以从用户的 GitHub repository clone 项目；
* 可以选择将结果保存到 Google Drive；
* 不在 notebook 中硬编码 token、密码或私人路径；
* 提供 quick reproduction 和 length experiment 两种入口；
* 运行完成后将 results、figures、logs、configs 和 provenance 打包为 zip；
* 提供下载 zip 的 cell；
* 告诉用户如何把 zip 解压回本地仓库；
* 不自动把大型参数、缓存或临时文件提交到 GitHub。

COLAB_RUNBOOK.md 必须逐步说明：

1. 打开 notebook；
2. 登录 Google；
3. 选择 GPU runtime；
4. 检查实际 GPU；
5. 挂载 Drive 是否必要；
6. 运行 setup；
7. 先运行 100 aa reproduction；
8. 检查输出；
9. 再运行 quick length experiment；
10. 下载结果；
11. 合并回仓库；
12. 运行 verify_outputs.py；
13. 生成图；
14. 检查 README；
15. 提交 GitHub。

====================
十三、git 与大文件规则
=============

.gitignore 至少排除：

* AlphaFold parameters
* model weights
* caches
* temporary files
* Python cache
* Jupyter checkpoints
* large archives
* Google Drive mount paths
* environment secrets
* IDE private state

不要排除：

* 小型最终 PDB
* FASTA
* metrics CSV
* config YAML
* figures
* provenance
* logs 中用于复现的必要信息

如果某个文件过大，不要直接提交。记录下载来源和 SHA256。

在没有用户授权或没有 GitHub 认证时：

* 可以初始化本地 git；
* 可以准备 commits；
* 不要猜测远程仓库地址；
* 不要强行 push；
* 输出用户需要执行的 GitHub 操作。

====================
十四、执行顺序
=======

严格按以下顺序工作：

Phase 1

* 环境审计
* 阅读官方资源
* 记录当前 API 与版本
* 创建目录结构

Phase 2

* 创建配置系统
* 创建结果 schema
* 创建 provenance 系统
* 创建轻量测试

Phase 3

* 整理官方 reproduction notebook
* 创建 Colab workflow
* 创建本地运行入口
* 运行静态测试和 dry-run

Phase 4
如果 GPU 可用：

* 运行 100 aa reproduction
* 验证输出
* 修复真实错误
* 收集 metrics
* 生成第一批图

如果 GPU 不可用：

* 停止正式计算
* 完成 COLAB_RUNBOOK.md
* 把项目留在“ready for GPU execution”状态

Phase 5
仅当 100 aa reproduction 真实成功后：

* 运行 quick length experiment
* 收集并检查结果
* 生成真实图表
* 更新 README Results 和 Observations

Phase 6

* 运行完整测试
* 运行 verify_outputs.py
* 检查 git status
* 检查是否错误提交大型文件或凭据
* 输出最终工作报告

====================
十五、你需要向我报告什么
============

完成后不要只说“done”。请输出：

1. 环境判断：

   * 本地 GPU 是否可用
   * 选择了哪个执行后端
   * 哪些步骤实际执行了

2. 创建/修改的文件列表。

3. 官方 RSO notebook 中确认到的关键设置：

   * 版本/commit
   * loss
   * optimization steps
   * ProteinMPNN 设置
   * validation model

4. 测试结果：

   * 哪些通过
   * 哪些失败
   * 失败原因

5. 实验状态：

   * reproduction 是否真实完成
   * length experiment 是否真实完成
   * 实际结果路径
   * 不得把 dry-run 算作 reproduction

6. 阻塞项：

   * 缺 GPU
   * 显存不足
   * 网络失败
   * API 变化
   * 模型下载失败
   * 需要我手动登录或授权的部分

7. 下一步只列我本人必须完成的操作，并给出可以直接复制的命令。每条命令都解释参数。

8. 给出下面三个状态之一：

* READY_FOR_GPU_EXECUTION
* REPRODUCTION_COMPLETED
* QUICK_EXPERIMENT_COMPLETED

只有存在真实、通过验证的计算结果时，才允许使用后两个状态。

现在开始。先做环境审计和官方 notebook/API 检查，不要在尚未确认 API 的情况下直接生成大量实现代码。
